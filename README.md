# CF-app

This is a React Native app for discovering and managing meals, featuring categories, meal details, and favorites.

## Features

- Browse meals by categories
- View detailed recipes
- Add/remove favorites

## Setup

1. Install dependencies: `npm install`
2. Run the app: `expo start`
